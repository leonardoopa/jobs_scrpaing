version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: jobscrap_postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: adminpassword
      POSTGRES_DB: jobscrap_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:alpine
    container_name: jobscrap_redis
    restart: always
    ports:
      - "6379:6379"

volumes:
  postgres_data:
